# Test Todo App

This repository starts with orchestration only. The todo app itself should be
built by the agent from a ticket using the workflow in `.github/workflows/agent.yml`.

Use the first trial ticket to ask for the initial app.

